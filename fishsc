getgenv().config = {
    autoFish = true, -- auto fish 
    autoPresents = true, -- collect presents
    placetoFish = "AdvancedFishing", -- area to fish
    invisWater = true, -- invis water

    userToMail = "comer_item", -- username to automail to
    autoMail = true, -- automail
    sendHuges = true, -- automail huges (corgis)
    sendShards = true, -- automail shards
    sendDiamonds = true, -- automail diamonds

    minShards = 150, -- minimum shards to send (when sendShards = true)
    minDiamonds = 3100000, -- minimum diamonds to send (when sendDiamonds = true)
    keepDiamonds = 100000, -- amount of diamonds to keep
}
loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/29ed908d75b673180ef22ff96a3b9800.lua"))()
